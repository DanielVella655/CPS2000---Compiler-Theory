# CPS2000 - Compiler Assignment

This compiler for the ResIR language was built in OCaml, divided into 5 files that each individually achieve a task described by the assignment spec. The code_generator.ml file contains the gen_program_opt function that takes as input the ResIR source code as a string and returns the equivalent C11 code after Dead Code Elimination.

