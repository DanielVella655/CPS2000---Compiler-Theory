extern type Geometry::Point {
  x : i32;
  y : i32;
}

function bad_member_ptr(%point_ptr: ptr<Geometry::Point>, %new_x: i32) -> void {
  locals {
    %x_ptr : ptr<i32>;
    %test_ptr : ptr<bool>;
  }
  entry bb1;

  bb1:
    %x_ptr = member_ptr %test_ptr, x;
    store %x_ptr, %new_x;
    return;
}